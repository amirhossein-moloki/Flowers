import { Entity } from './entity';

export abstract class BaseEntity<T> extends Entity<T> {
  // Common properties like id, createdAt, updatedAt can be defined here
  // This is a placeholder for any base entity logic you might need.
}